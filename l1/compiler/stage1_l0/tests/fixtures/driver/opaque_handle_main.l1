/*
 * SPDX-License-Identifier: MIT OR Apache-2.0
 * Copyright (c) 2026 gwz
 */

module opaque_handle_main;

import opaque_handle_dep;

func main() -> int {
    let handle = make_handle(23);
    if (same_handle(handle, handle)) {
        return 23;
    }
    return 1;
}
