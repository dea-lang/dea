/*
 * SPDX-License-Identifier: MIT OR Apache-2.0
 * Copyright (c) 2026 gwz
 */

module opaque_import_dep;

export opaque { Handle, State }, make_handle;

struct Handle {
    value: int;
}

enum State {
    Idle;
    Running;
}

func make_handle(value: int) -> Handle* {
    return new Handle(value);
}
