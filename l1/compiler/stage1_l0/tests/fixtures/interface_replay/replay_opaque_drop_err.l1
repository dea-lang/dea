/*
 * SPDX-License-Identifier: MIT OR Apache-2.0
 * Copyright (c) 2026 gwz
 */

module replay_opaque_drop_err;

import replay_provider;

func main() -> int {
    let point = allocate_point();
    let handle = make_handle();
    let token = make_token();
    drop point;
    drop handle;
    drop token;
    return 0;
}
